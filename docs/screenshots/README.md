# Screenshots for submission

Capture these after indexing a few PDFs and running the app at http://localhost:7860:

1. `chat-citations.png` - Chat tab with an answer and the References panel
2. `knowledge-base.png` - Knowledge base tab after a successful index
3. `vector-2d.png` - Vector space 2D plot with cluster colors
4. `vector-3d.png` - Vector space 3D plot with cluster colors

Optional: a short screen recording of index -> ask -> viz.

Keep secrets out of frames (no `.env` contents).
